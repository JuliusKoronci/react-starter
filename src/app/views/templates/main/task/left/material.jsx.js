import React, {PropTypes} from 'react';

const material = (prop) => {
    return (
        <div></div>
    );
};

material.propTypes = {
    prop: PropTypes.object.isRequired
};

export default material;