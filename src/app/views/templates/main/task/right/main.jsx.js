import React, {PropTypes} from 'react';

const main = ({task}) => {
    return (
        <div>prava sekcia</div>
    );
};

main.propTypes = {
    task: PropTypes.object.isRequired
};

export default main;