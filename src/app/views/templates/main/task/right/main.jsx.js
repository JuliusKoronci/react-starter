import React, {PropTypes} from 'react';

const main = (prop) => {
    return (
        <div>prava sekcia</div>
    );
};

main.propTypes = {
    prop: PropTypes.object.isRequired
};

export default main;