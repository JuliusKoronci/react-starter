import React, {PropTypes, Component} from 'react';
import View from '../../../../views/templates/main/settings/roles/add_role.jsx';

class Role extends Component {

    constructor(props, context) {
        super(props, context);
    }

    render() {
        return (
            <View prop={{}} />
        );
    }
}

Role.propTypes = {
    //myProp: PropTypes.string.isRequired 
};


export default Role;
