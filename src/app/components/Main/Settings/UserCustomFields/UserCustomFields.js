import React, {PropTypes, Component} from 'react';
import View from '../../../../views/templates/main/settings/user_custom_fields/user_custom_fields.jsx.js';

class UserCustomFields extends Component {

    constructor(props, context) {
        super(props, context);
    }

    render() {
        return (
            <View prop={{}} />
        );
    }
}

UserCustomFields.propTypes = {
    //myProp: PropTypes.string.isRequired 
};


export default UserCustomFields;
